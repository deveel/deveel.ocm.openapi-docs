{
  "name": "deveel-omni-channel-messaging-v1",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "*"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi lint"
  }
}
