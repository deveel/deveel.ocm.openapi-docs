type: object
properties:
  label:
    minLength: 2
    pattern: '^[a-zA-Z][a-zA-Z0-9]*$'
    type: string
    nullable: true
  email:
    type: string
    format: email
    nullable: true
  displayName:
    type: string
    nullable: true
  number:
    type: string
    format: tel
    nullable: true
additionalProperties: false
